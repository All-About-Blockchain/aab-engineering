{
  "name": "aab-engineering",
  "version": "1.0.0",
  "private": true,
  "description": "Building Blocks for Agentic DeFi",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "deploy:staging": "turbo run deploy:staging",
    "deploy:prod": "turbo run deploy:prod"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
